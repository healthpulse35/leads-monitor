# URL of the Apps Script web app (§3.2) that returns the leads JSON.
# Copy this file to `.env` and paste your deployed web-app URL.
# When unset, the app runs entirely off the bundled fixture (public/leads-sample.json).
VITE_DATA_URL=
